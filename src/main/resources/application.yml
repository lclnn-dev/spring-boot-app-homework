# Spring Boot configuration
spring:
  #application:
  # name: service
  #main:
  # allow-bean-definition-overriding: true
  # lazy-initialization: true

  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/employee
    username: postgres
    password: pass

  # JPA properties
  jpa:
    hibernate:
      ddl-auto: none # When you launch the application for the first time - switch "none" at "create"

    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    #open-in-view: false
    #generate-ddl: true
    properties:
     org:
      hibernate:
       envers:
#        default_schema: your_audit_schema
        audit_table_suffix: _HISTORY

  flyway:
    #    locations: filesystem:.\src\main\resources\db\migration
    enabled: false
    #    clean-disabled: true
    baseline-on-migrate: true
  #  security:
  #    user:
  #      name: admin
  #      password: admin

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-1.0.xml

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GOOGLE_MAIL_USERNAME}
    password: ${GOOGLE_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: false
          starttls.enable: false

# Logger configuration
logging:
  pattern:
    console: "%d %-5level %logger : %msg%n"
  level:
    org.springframework: info
    org.hibernate: info
    com.example.demowithtests: debug

# Server configuration
server:
  port: 8088

# Swagger configuration
springdoc:
  swagger-ui:
    path: /swagger-ui.html # swagger-ui custom path
    operationsSorter: method
  api-docs:
    path: /v3/api-docs.yaml
management:
  endpoints:
    enabled-by-default: false
  endpoint:
    health:
      enabled: true

photopass:
  storageDir: E:/test-photo

history-data:
  work-pass: E:/history-data/history-data-pass.txt
